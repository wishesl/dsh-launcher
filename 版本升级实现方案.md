# 启动器「版本升级」实现方案

> 目标：给 DSH Launcher 自己加一条**应用内更新闭环** —— 检查 GitHub Releases → 有新版本就在线下载 → 校验 → 就地替换 → 生效。
> 本文只做设计（先设计、后实现），所有结论都对照仓库现状与实测验证，不写"应该能行"。
>
> **一句话结论**：检查与下载用现成的 `proxyHTTPClient` + GitHub 公开 API 即可；**Windows 就地自替换已在
> 本机 spike 验证通过**（重命名运行中的 exe → 新 exe 放回原路径 → 老进程照常运行），所以默认方案**不需要
> helper 进程、不需要等重启**。入口与生效时机已拍板（§10）：**点顶栏版本 pill → 弹窗**（弹窗内自带完整日志），
> **静默替换 + 下次启动生效**（另给「立即重启生效」按钮）。

---

## 1. 结论先行

1. **检查更新 = 一次 HTTP + 一次版本号比较。** `GET https://api.github.com/repos/wishesl/dsh-launcher/releases/latest`
   → `tag_name`（如 `v0.5.1`）与 `main.version`（`version.go`，CI 由标签注入）比较即可。不需要新框架。
2. **Windows 自替换已验证可行（§4.1）。** 运行中的 exe 允许被**重命名**，把新 exe 放回原路径后老进程继续
   正常跑（心跳不断），下次启动就是新版；老进程退出后再删 `.old` 也成功。→ **默认「静默替换 + 下次启动生效」**
   是最省事、最不打扰用户的路径（不会像"立即重启"那样把正在跑的 DSH 实例一起停掉）。
3. **必须 fail-open。** api.github.com 有 60 次/小时/IP 的匿名限流（本机实测当时只剩 19/60），网络也不一定通。
   按 AGENTS.md §9 的规矩：**没有结论就是 `unknown`（中性灰），不标红、不拦功能**；更新检查失败绝不能影响
   启动器其它能力。

---

## 2. 需求核对（是什么 / 为什么 / 怎么做）

| 项 | 内容 |
|---|---|
| **是什么** | 新增「版本升级」入口：**点顶栏版本 pill 弹窗**（+ 设置页 / 托盘二级入口），展示 当前版本 / 最新版本 / 更新说明 / 下载进度与日志，并提供「下载并安装」「立即重启生效」「打开 Release 页」「跳过此版本」。 |
| **为什么** | 现在只能自己去 GitHub Releases 下载：① 用户得先知道有新版；② 手工覆盖正在运行的 `dsh-launcher.exe` 会被 Windows 拒绝（文件被占用），普通用户会卡在"关掉再复制"；③ 没有校验，下到半截的 exe 也可能被用上。 |
| **怎么做** | Go 侧新增 `update.go`（check / download / verify / apply），复用 `proxyHTTPClient()` 与 `emit()` 事件通道；前端新增 `UpdateDialog.tsx`（自带完整日志）+ pill 可点 + 设置页一行 + 托盘一项；CI 补一个 `SHA256SUMS` 资产。 |

**不做**（本期明确排除，写在这里避免范围蔓延）：增量/差分更新、断点续传、代码签名与公证、后台静默自动下载、
插件/DSH 本体的更新（那是既有功能，互不相干）。

---

## 3. 现状盘点（本方案踩在上面的地基）

| 已有能力 | 位置 | 本方案怎么用 |
|---|---|---|
| 走代理的 HTTP 客户端 | `proxy.go: proxyHTTPClient(timeout)` | 检查（15s）与下载（长超时）都用它，自动吃 `settings.json` 的 `proxy` |
| 事件通道 + 单一订阅所有者 | `app.go: emit()` / `App.tsx` 的 effect | **新增 `dsh:update-log` / `dsh:update-status`**，与 market 两条一一对应，订阅仍在 App 集中管理 |
| 右栏常驻日志栏（三标签） | `LogDrawer.tsx` + `LogTab = 'logs' \| 'market' \| 'compat'` | **不动**：自更新日志走弹窗（§6.3 的显式例外），三标签约定原样保留 |
| 任务单飞 + 流式日志 + 可取消 | `market_ops.go`（`marketBusy` / `runMarketCommand` / `CancelMarketOp`） | 更新任务照抄这一套：`updateBusy` 单飞 + `CancelLauncherUpdate()` |
| 顶栏品牌区 / 版本 pill | `Header.tsx`：`.brand-ver`（`launcherVersion` 非空才渲染） | pill 改成可点按钮，有更新时变色 + 红点（mac 布局与内嵌态没有 pill，故配二级入口） |
| 设置页表单行 | `SettingsView.tsx`（代理 / 布局） | 加一行「关于与更新」作为稳定二级入口 |
| 版本号来源 | `version.go`：`var version = "dev"`，CI 用 `-ldflags "-X main.version=$VERSION"` 注入 | 比较的"当前版本"就取它；`dev` 特殊处理（§4.5） |
| Release 资产命名（已固定） | `.github/workflows/release.yml` | 按 `GOOS/GOARCH` 精确匹配资产名（§5.3） |
| 托盘菜单 | `tray.go`（`systray.AddMenuItem` / `SetTitle`） | 可选：加一项「检查更新」（M3） |
| 配置持久化 | `settings.json`（`settingsStore`） | 存 `lastUpdateCheck` / `skippedVersion` / `autoCheckUpdate` |

**已核对的事实（2026-09-13 实测）**：

```
GET /repos/wishesl/dsh-launcher/releases/latest → tag v0.5.1（prerelease=false, draft=false）
  dsh-launcher-windows-amd64.exe   12,696,064 B   application/x-msdos-program
  dsh-launcher-darwin-arm64.zip     4,134,747 B   application/zip
  dsh-launcher-darwin-amd64.zip     4,566,439 B   application/zip
  dsh-launcher-linux-amd64.tar.gz   4,468,465 B   application/gzip
  没有 SHA256SUMS 资产  ← M2 的前置依赖（见 §4.4）
rate_limit core: 19/60（匿名，按 IP）
```

---

## 4. 技术难点与验证

### 4.1 [难点A] Windows 就地替换运行中的 exe —— ✅ 已实测通过

担心的是"Windows 不允许覆盖正在运行的 exe"。实测结论：**覆盖不行，但重命名 + 放回原路径可以**，且老进程
不会被打断。spike（最小 Go 程序 + 真实进程，非纸面推演）：

```
旧进程 pid=5484 存活=True
--- 尝试重命名运行中的 exe ---     rename 运行中 exe: OK
--- 把新 exe 放到原路径 ---        copy 新 exe: OK
--- 旧进程是否还活着（换文件后）---  存活=True  心跳=tick=5 MARKER=OLD
--- 原路径上的文件现在是哪个版本 --- app.exe --once -> MARKER=NEW
--- 结束旧进程并清理 .old ---      旧进程退出: True / 删除 app.old.exe: OK
```

→ 落地动作（Windows）：

```
os.Rename(self, self+".old")          // ① 运行中的 exe 允许重命名
os.Rename(downloaded, self)           // ② 同卷 MoveFile，把新版放回原路径
// ③ 老进程照常运行；下次启动即为新版；.old 在下次启动时（或替换成功后）尽力删除
```

失败回滚：① 成功而 ② 失败 → 把 `.old` 改回原名并报错；两步都失败 → 保持原状报错（**绝不留下"没有 exe"的状态**）。

### 4.2 [难点B] 「立即重启」时的单实例锁竞争

Wails 的单实例是 `wails.Run` 内部 `SetupSingleInstance()` 建的**命名互斥体** `wails-app-dsh-launchersim`
（已读 `wails@v2.15.0/internal/frontend/desktop/windows/single_instance.go`）：第二个进程发现互斥体存在 →
给第一个进程发 `WM_COPYDATA` → **自己 `os.Exit(0)`**。

所以：新 exe 必须在**旧进程退出之后**才进 `wails.Run`，否则会被当成"第二实例"直接自杀。做法：

```
main(): 若 os.Args 含 --updated-from=<pid>
        → 先等这个 pid 消失（≤30s 兜底），再 wails.Run(...)
        → 等待期间不初始化 tray / 不建 WebView
```

pid 等待用 `OpenProcess(SYNCHRONIZE) + WaitForSingleObject`（`golang.org/x/sys/windows` 已在依赖树里，
`go.mod` 间接引入；`syscall` 亦可用），非 Windows 用 `syscall.Kill(pid, 0)` 轮询。
**这条是实现期要补的第二个 spike**（本次只验证了更关键的 A）。

> 注意：`runtime.Quit` 会走 `App.shutdown()` → **停掉所有启动器托管的 DSH 实例**（`app.go:163-175`）。
> 所以"立即重启"必须显式提示用户（§6.4），这也是默认方案选"静默替换 + 下次启动生效"的原因。

### 4.3 [难点C] 网络可达性 / 限流 / 代理

- 匿名 API 限流 60 次/小时/IP（实测只剩 19）。对策：**内存缓存 10 分钟** + `If-None-Match`（ETag 命中 304
  **不计入**限流）+ 只在用户点「检查更新」或启动后延迟一次时请求；禁止定时轮询。
- 强制带 `User-Agent: dsh-launcher/<version>`（GitHub API 要求，否则 403）。
- api.github.com 与资产下载都走 `a.proxyHTTPClient()`；资产会 302 到 `objects.githubusercontent.com`，
  Go 的 `http.Client` 跟随跳转并复用同一个 Transport（代理照样生效）。
- 失败一律 `unknown`：文案是"暂时查不到（可点重试 / 打开 Release 页）"，**不是红色错误**。

### 4.4 [难点D] 完整性校验：现有 Release 没有校验和

已核对：v0.5.1 的 4 个资产里**没有 `SHA256SUMS`**。所以：

- CI 改动（**M2 前置**）：在 `release.yml` 的 release job 里生成并上传 `SHA256SUMS`（`sha256sum dist/*`，
  Windows 侧用 pwsh 的 `Get-FileHash`，或统一在 ubuntu 的 release job 里对下载下来的产物计算，避免跨平台格式差异）。
- 客户端：下载后比对 `SHA256SUMS` 里对应资产的哈希；**只信任同一次 API 响应里拿到的资产 + 哈希文件**
  （信任锚 = GitHub TLS + 仓库归属，不做代码签名，这一点必须在文档/UI 里说清）。
- 同时校验 `asset.size` 与实际字节数；不一致即删文件报错，绝不应用。

### 4.5 [难点E] `version=dev` 的本地构建

源码构建（`wails build` 不带 ldflags）时 `version == "dev"`。**dev 构建不参与比较、不显示「有更新」**，
只显示一行灰字：`本地构建（未注入版本号），跳过版本比较`。否则每个开发者本地都永远"有新版本"。

---

## 5. 后端设计：`update.go`

### 5.1 常量与类型

```go
const (
    releaseRepo     = "wishesl/dsh-launcher"   // 可被 settings 覆盖（fork 场景）
    latestAPI       = "https://api.github.com/repos/" + releaseRepo + "/releases/latest"
    releasesAPI     = "https://api.github.com/repos/" + releaseRepo + "/releases?per_page=10"
    updateCheckTTL  = 10 * time.Minute
)

// LauncherRelease 是"启动器自己"的版本视图（与 DSH 的 RegistryInfo 无关）。
type LauncherRelease struct {
    Current     string `json:"current"`      // main.version（dev 原样返回）
    Latest      string `json:"latest"`       // 去掉 v 前缀，如 0.5.1
    HasUpdate   bool   `json:"hasUpdate"`
    Comparable  bool   `json:"comparable"`   // false = dev/未知版本，不比较
    Prerelease  bool   `json:"prerelease"`
    Name        string `json:"name"`
    Notes       string `json:"notes"`        // release body（原样文本）
    PublishedAt string `json:"publishedAt"`
    HTMLURL     string `json:"htmlUrl"`
    Asset       AssetRef `json:"asset"`      // 本平台对应资产；不支持时为零值
    Supported   bool   `json:"supported"`    // 当前平台能否应用内更新
    CheckedAt   string `json:"checkedAt"`
    Cached      bool   `json:"cached"`
    Err         string `json:"err"`          // 非空 = 没结论（UI 显示中性灰）
}

type AssetRef struct{ Name string `json:"name"`; Size int64 `json:"size"`; URL string `json:"url"` }
```

### 5.2 绑定方法（全部导出 → `window.go.main.App.*`）

| 方法 | 说明 |
|---|---|
| `CheckLauncherUpdate(force bool) LauncherRelease` | 10 分钟缓存 + ETag；`force` = 用户点「检查更新」。**永不返回 error**（失败写进 `Err` 字段），前端只需一种渲染路径 |
| `DownloadLauncherUpdate() error` | 单飞下载到 `updates/`，流式发 `dsh:update-log` + `dsh:update-status`（含百分比） |
| `CancelLauncherUpdate() bool` | 取消下载（`context.CancelFunc` + 删半截文件），与 `CancelMarketOp` 同构 |
| `ApplyLauncherUpdate() error` | 校验 → 替换（§4.1）→ 视模式重启（§6.4） |
| `UpdateOpRunning() bool` | 前端挂载时恢复"正在下载"状态（对齐 `MarketOpRunning`） |
| `OpenReleasePage()` | 兜底：`runtime.BrowserOpenURL(htmlUrl)`（mac/linux 或权限不足时用） |
| `GetUpdateSettings() / SetUpdateSettings(s)` | `autoCheck`（启动后延迟自检，默认开）、`includePrerelease`（默认关）、`skippedVersion`、`sourceRepo` |

### 5.3 资产选择（按 `GOOS/GOARCH` 精确匹配，不做模糊猜）

| 平台 | 资产名 | 应用内更新 |
|---|---|---|
| windows/amd64 | `dsh-launcher-windows-amd64.exe` | ✅ 就地替换 |
| darwin/arm64 | `dsh-launcher-darwin-arm64.zip` | ❌ 本期：`打开下载页` |
| darwin/amd64 | `dsh-launcher-darwin-amd64.zip` | ❌ 本期：`打开下载页` |
| linux/amd64 | `dsh-launcher-linux-amd64.tar.gz` | ⭕ M4：解包替换二进制（本期先给下载页） |

找不到对应资产 → `Supported=false`，UI 只给「打开 Release 页」。

### 5.4 下载

- 目录：`%APPDATA%\DSHLauncher\updates\`（与 `instances.json` / `settings.json` 同层，用 `os.UserConfigDir()`）；
  文件 `dsh-launcher-<latest>.exe.part` → 校验通过后改名去掉 `.part`。
- 进度：`io.Copy` 包一层 counting writer，**节流 200ms** 发一次 `dsh:update-status`（避免刷爆事件通道）。
- 超时：整体 `context` 15 分钟（大文件 + 慢代理），单次读超时靠 Transport；取消即断。
- 失败重试：整包重下一次（不做断点续传）；`.part` 残留在下一次下载前清理。
- **可写性预检**（M2）：替换前在 exe 同目录试建一个临时文件再删掉；失败 → 明确提示
  "启动器所在目录不可写（可能是 Program Files），请用「打开 Release 页」手动替换"。

### 5.5 版本比较

`compareVersions(a, b string) int`：复用 `util.go: parseVersion()` 的 semver 正则做校验，按
`主.次.补` 数值比较，**预发布按 semver 规则排序**（`1.5.0-rc.1 < 1.5.0`），本地高于线上 → 显示"本地版本更新"且
不给更新按钮（与 `插件更新功能实现方案.md` §3.1 的判定口径保持一致）。

---

## 6. 前端设计

### 6.1 入口形态（**决策 D1，已拍板：点版本 pill → 弹窗**）

**为什么不能只做 pill**：pill 只在 win 布局存在，另外两种场景根本没有入口 —— 所以必须配二级入口。

| 场景 | 版本 pill 可见？ | 入口 |
|---|---|---|
| win 布局（`os-win`） | ✅ 顶栏品牌区 | **pill 点击** |
| mac 布局 | ❌ `.app-header .header-brand { display:none }`（`style.css:89`），侧边栏 `.side-head` 的 brand **没有 pill** | 设置页「关于与更新」/ 托盘 |
| 内嵌 DSH 视图 | ❌ 品牌块整块不渲染（`Header.tsx` 的 `!embedMode` 条件，昨天刚做的） | 设置页 / 托盘 |

- **主入口**：`Header.tsx` 的 `.brand-ver` pill 变成可点按钮（`cursor:pointer` + hover 边框 +
  `title="点击查看版本与更新"`）；**有更新时 pill 变强调色 + 红点**（这是唯一的"有新版"信号），点击打开 `UpdateDialog`。
- **二级入口**：`SettingsView` 加一行「关于与更新」（当前版本 + 「检查更新」按钮）；`tray.go` 加一项「检查更新」
  （`systray.AddMenuItem`，与既有 `实例` 子菜单同层）。
- **不新增 `ViewKey`、不动 `Sidebar.tsx`、不动 `App.tsx` 的三栏宽度逻辑** —— 改动面最小。
- **为什么不做侧边栏菜单**：① 会和「版本历史」（那是 **DSH** 的版本）并排，极易点错；②「下载并安装启动器」是低频
  系统级动作，不该占一个常驻主导航位。

### 6.2 弹窗 `UpdateDialog.tsx`（**决策 D3，已拍板：弹窗内自带完整日志**）

复用 `ExitDialog` / `ShareCodeDialog` / `InstanceForm` 已有的 modal 遮罩与卡片样式（`style.css` 现有规则），
**不引新依赖、不引 markdown 库**：

```
┌─ DSH Launcher 更新 ──────────────────────────────────── ✕ ┐
│ 当前 v0.5.1   →   最新 v0.5.2  ● 有新版本                 │
│ 发布于 2026-09-13 20:57 · [打开 Release 页]               │
│ ┌─ 更新说明（pre-wrap，可滚动，最大高 160px）───────────┐ │
│ │ …release body…                                        │ │
│ └───────────────────────────────────────────────────────┘ │
│ [下载并安装] [跳过此版本] [立即重启生效]                   │
│ ████████████░░░░░░  62%  7.9 MB / 12.7 MB   [取消]        │
│ ┌─ 下载日志（等宽，180px，自动滚到底，[复制] [清空]）───┐ │
│ │ 目标资产 dsh-launcher-windows-amd64.exe (12.7 MB)     │ │
│ │ 写入 C:\…\DSHLauncher\updates\dsh-launcher-0.5.2…part │ │
│ │ sha256 校验通过                                       │ │
│ │ 已就位：退出启动器后，下次打开即为 v0.5.2             │ │
│ └───────────────────────────────────────────────────────┘ │
│ ☑ 启动时自动检查更新   ☐ 包含预发布版本（rc/beta）        │
└────────────────────────────────────────────────────────────┘
```

**三条必须遵守的弹窗行为**：

1. **关弹窗 ≠ 取消任务**（与 `InstanceForm` 既有语义一致）：下载继续跑，pill 上显示进度/红点，重新点开接着看日志；
   要停只能点弹窗里的「取消」。
2. **Esc / 点 ✕ / 点遮罩只关界面**，不打断 `DownloadLauncherUpdate()`；重开时靠 `UpdateOpRunning()` + 事件恢复状态。
3. **三态文案**（对齐 AGENTS.md §9.2）：**已是最新**（中性）/ **暂时查不到**（中性灰 + 重试）/
   **本平台不支持应用内更新**（中性 + 「打开 Release 页」）；`dev` 构建显示「本地构建（未注入版本号），跳过比较」。

### 6.3 日志归属：对 AGENTS.md §0.1 的**显式例外**（已拍板）

AGENTS.md §0.1 要求"进度/日志输出必须进右侧第三栏、禁止覆盖式浮层"。本功能按你的决定**破例**，理由与边界：

- 自更新是**系统级动作**，与"实例/插件市场/兼容性"这三个上下文都无关；塞进右栏会污染标签语义
  （右栏三个标签全都挂在"当前实例"这个上下文上）。
- 所以：**弹窗内自带完整日志**（`dsh:update-log` 直接渲染在弹窗里），不依赖右栏；
  **`LogTab` 保持 `'logs' | 'market' | 'compat'` 三标签不变**（AGENTS.md §3 的约定不动）。
- 落地时同步在 AGENTS.md §0.1 追加一句边界说明，避免以后被当成"违规"改回去：

  > **例外**：启动器自更新弹窗（`UpdateDialog`）自带完整日志 —— 它是系统级动作，与三栏的内容上下文无关。
  > 除这一处外，进度 / 日志输出仍一律进右侧第三栏，仍然禁止覆盖式抽屉。

事件契约（与 market 一一对应，`App.tsx` 仍是唯一订阅所有者）：

| 事件 | 载荷 | 用途 |
|---|---|---|
| `dsh:update-log` | `{ line: string }` | 下载/校验/替换的逐行输出 |
| `dsh:update-status` | `{ state: 'running'\|'done'\|'failed'\|'cancelled', phase: 'download'\|'verify'\|'apply', percent: number, bytes: number, total: number, error?: string }` | 进度 + 状态；`done` 时主区切成"已就绪，重启生效" |

**弹窗关闭后的状态出口**：下载在后台继续，版本 pill 上显示进度（`62%`）或红点；重新点开弹窗即可看到完整日志。
不自动打开右栏日志栏（§6.3 的例外）。

### 6.4 生效时机（**决策 D2，已拍板：默认静默替换 + 另给「立即重启生效」按钮**）

| 选项 | 行为 | 代价 |
|---|---|---|
| **A. 静默替换 + 下次启动生效（推荐）** | 替换完提示"新版本已就绪，退出后下次打开即生效" | 用户可能一直不退出启动器；但**不打断任何正在跑的 DSH 实例** |
| B. 替换 + 立即重启 | 调 `RestartLauncherNow()`：等旧 pid 退出 → 新进程接管 | **会停掉所有托管的 DSH 实例**（`shutdown()` 的既有语义），必须二次确认 |
| C. 两者都给（A 为默认，按钮提供 B） | 灵活性最好 | 多一个按钮 + 一条二次确认文案 |

推荐 **A 默认 + B 作为显式按钮**（即 C 的形态，但默认路径是 A），并在 B 的确认框里写明
「将停止 N 个正在运行的实例（标记了『随启动器自动启动』的会在新版本启动后自动拉起）」。
**已按此拍板**（§10 D2）。

### 6.5 状态管理

`App.tsx` 提升：`updateInfo: LauncherRelease | null`、`updateOp: UpdateOpState`、`updateLogs: string[]`
（对齐 `marketOp` / `marketLogs` 的既有形态），弹窗由 `updateOpen: boolean` 控制显隐；回调一律 `useCallback`
稳定化（AGENTS.md §4.1 踩过的坑）。`types.ts` 同步加 `LauncherRelease` / `UpdateOpState`。

---

## 7. 实施分期

| 里程碑 | 内容 | 可独立验收 |
|---|---|---|
| **M0（前置）** | `release.yml` 产出 `SHA256SUMS`（§4.4） | 下一次 Release 能看到该资产 |
| **M1 只读检查 + 入口** | `update.go` 的 `CheckLauncherUpdate` + 缓存/ETag/代理；版本 pill 可点（有更新变色 + 红点）；`UpdateDialog` 弹窗（当前/最新版本、更新说明、三态文案、打开 Release 页）；设置页「关于与更新」一行；托盘「检查更新」；AGENTS.md §0.1 例外说明 | 有新版时 pill 变色、弹窗正确显示；断网时中性灰不报错；`dev` 构建不参与比较；mac 布局 / 内嵌态也能从设置页进 |
| **M2 下载与替换** | 下载 + 进度事件 + **弹窗内完整日志** + sha256 校验 + 可写性预检 + Windows 就地替换 + 下次启动生效 + `.old` 清理 | 在真机上从 v0.5.1 升到下一版，退出重开看到新版本 pill |
| **M3 重启与自动化** | `--updated-from=<pid>` 等待 + 「立即重启生效」+ 二次确认 + 启动后延迟 8s 自检（可关） | 立即重启路径不会撞单实例锁；DSH 实例影响有明确提示 |
| **M4 跨平台** | macOS（zip → `.app` 替换）/ Linux（tar.gz → 二进制替换）；不支持的平台保持下载页兜底 | mac/linux 各自真机验证 |
| **M5 打磨（可选）** | 「跳过此版本」、更新说明排版、"本次运行已检查"节流、代理不可用时的直连回退 | —— |

**测试计划**

- Go：`update_test.go` —— `compareVersions` 表驱动（含预发布/相等/dev）、资产选择（GOOS/GOARCH 表）、
  404/403/限流/超时的处理分支、sha256 不匹配必须拒绝、替换逻辑在临时目录里模拟（含回滚）。
- 前端：Playwright + stub（复用 AGENTS.md §6 的 `window.runtime` / `window.go.main.App` 注入），断言三态渲染、
  进度事件驱动弹窗 UI、**关弹窗后任务继续**、`dev` 文案。
- 真机：M2 的替换路径必须**真跑一次**（这是本方案唯一无法用单测替代的部分）。

---

## 8. 安全与信任边界（说清楚，不假装）

1. **信任锚**：GitHub HTTPS + 仓库归属。哈希文件与资产来自同一次 API 响应，因此能防"下载被截断/被中间人改写
   单个字节"，但**防不住仓库本身被投毒**（无代码签名、无公证）。
2. **不做静默自动升级**：必须用户点击才会下载与应用（"启动时自动检查"只检查、不下载）。
3. **不写系统目录**：只在 exe 同目录替换，且替换前做可写性预检；失败给下载页兜底。
4. **不留半截文件**：`.part` / `.old` 在下次启动时清理，清理失败只记日志、不报错（AGENTS.md §9 的"不确定就不下结论"）。
5. **不做提权**：需要管理员权限的场景一律走"打开 Release 页"。

---

## 9. 与现有约定的关系

- **布局**：本功能是 AGENTS.md §0.1 的**唯一显式例外** —— 自更新弹窗自带完整日志（§6.3），其余任何新功能的进度 /
  日志输出仍一律进右栏第三列，仍然禁止覆盖式抽屉。
- **事件**：新增两条事件，但**订阅仍集中在 `App.tsx`**，子组件不自行 `EventsOn`（AGENTS.md §4.1）。
- **三态**：查不到 = `unknown`，不是 fail（AGENTS.md §9.2）。
- **版本号**：只读 `main.version`（ldflags 注入），**不改 `version.go`**（AGENTS.md §5）；`bestFitDSHVersion` 与本功能无关。
- **收尾**：`npm run build` + `wails build`（AGENTS.md §5）；M1 附带改一处 AGENTS.md §0.1（例外说明）。

---

## 10. 决策记录（已拍板）

| # | 决策 | 结论 | 落点 |
|---|---|---|---|
| D1 | 入口形态 | **点顶栏版本 pill → 弹窗**；配二级入口：设置页「关于与更新」+ 托盘「检查更新」（mac 布局与内嵌态没有 pill） | §6.1 |
| D2 | 生效时机 | **默认静默替换 + 下次启动生效**；另给「立即重启生效」按钮（二次确认，写明会停 DSH 实例） | §6.4 |
| D3 | 日志归属 | **弹窗内自带完整日志**（对 AGENTS.md §0.1 的显式例外）；`LogTab` 三标签不变 | §6.2 / §6.3 |
| D4 | 预发布渠道 | 默认只看 stable，加「包含预发布版本」开关（默认关） | §5.2 |
| D5 | 检查时机 | **启动后延迟 8s 自检一次（可关）+ 手动检查**；10 分钟缓存 + ETag | §4.3 |
| D6 | CI 加 `SHA256SUMS` | **必须**（M2 前置，随 M0 一起做） | §4.4 |

---

## 11. 下一步

1. 决策已定（§10 全部有结论）；
2. 已按 M0 → M1 → M2 → M3 落地（见 §12 的实现状态）；
3. 仍待真机：用带 `SHA256SUMS` 的**下一个真实 Release** 跑一次完整替换（v0.5.1 没有校验和资产）。

---

## 12. 实现状态（2026-09-13）

| 里程碑 | 状态 | 落地位置 |
|---|---|---|
| M0 CI 出 `SHA256SUMS` | ✅ | `.github/workflows/release.yml` 的 release job 新增 `Generate SHA256SUMS` |
| M1 检查 + 入口 | ✅ | `update.go: CheckLauncherUpdate`（10 分钟缓存 / ETag / 代理 / 三态）；版本 pill 可点 + 红点（`Header.tsx`）；`UpdateDialog.tsx`；`SettingsView` 的「关于与更新」；`tray.go` 的「检查更新」+ `dsh:update-open` |
| M2 下载 + 校验 + 就地替换 | ✅ | `update.go: DownloadLauncherUpdate`（download→verify→apply 三阶段事件）；`applyUpdateExecutable`（重命名运行中的 exe + 失败回滚）；`.old` / `.part` 启动清理 |
| M3 重启 + 自检 | ✅ | `main.go` 的 `--updated-from=<pid>` 等待 + `update_wait_windows.go` / `update_wait_other.go`；`RestartLauncherNow()`；启动后延迟 8s 自检（可关） |
| M4 跨平台 | ✅ | `update_apply.go`：`stageUpdatePayload`（按资产名解包）+ `applyUpdate`（按平台分流）。macOS 用 `ditto` 打的 zip 里的 `.app` **整包替换**（沿用用户原来的包名，跨卷退化成复制）；Linux 用 tar.gz 里的裸二进制替换；两者都走"重命名运行中的目标 → 新内容写回原路径 → 失败回滚" |

**M4 的实现要点（都是踩过才写下来的）**

- **解包先整体验路径**：tar.gz 要**两遍扫描**（第一遍验完整包再解），只在遇到目标文件时边扫边取会提前
  `return`，排在后面的 `../` 恶意条目根本不会被检查到 —— 这个洞是 Linux 上跑测试才露出来的。
- **符号链接/权限位**：CI 改用 `ditto` 打 mac 包；解包时按 zip 的 mode 还原符号链接，并给
  `Contents/MacOS/*` 补可执行位（老式 zip 可能没带）。
- **整包替换**：`<bundle>` → `<bundle>.old`，新包搬到原路径（**名字沿用原来的**），失败回滚；
  跨卷（`%APPDATA%` 与 `/Applications` 不同盘）自动退化成递归复制。
- **启动清理**：`.old` 可能是**目录**，`staged-*` 也要递归删。
- **替换策略三平台同一条**：改名运行中的目标（Windows 不允许覆盖运行中的 exe，Linux 写会 ETXTBSY，
  macOS 的 .app 同理），老进程继续跑完自己这一条命。

**验证矩阵**

| 平台 | 资产形态 | 验证方式 |
|---|---|---|
| windows/amd64 | 裸 exe | 真机 E2E（0.5.1→0.5.2、0.5.2→0.5.3，含直连无代理）+ 单测 |
| linux/amd64 | tar.gz | **WSL Ubuntu 上跑真机 E2E**：真实下载 `dsh-launcher-linux-amd64.tar.gz`、压缩包 sha256 与 `SHA256SUMS` 一致、解包后落地的二进制 11,029,024 字节；整套 Go 测试也在 Linux 上跑通 |
| darwin/arm64·amd64 | zip（.app 包） | ⚠️ 只有单测 + 假 GitHub 集成测试（用真实 zip 形态、含符号链接/权限位）。**没有 macOS 机器，真机 E2E 未做** —— 首次发版后请在 mac 上点一次 |

**验证证据**

- `go test ./...` 全绿。`update_test.go` 覆盖：资产名映射、三态构造、`SHA256SUMS` 解析、
  下载哈希与大小校验、替换成功与**回滚**、`--updated-from` 解析，以及**假 GitHub 的全链路**
  （检查 → 下载 → 校验 → 替换）与**校验和不匹配必须拒绝且不动原 exe**。
- `npm run build` + `wails build` 通过（产物 `build/bin/dsh-launcher.exe`）。
- Playwright 桩验证：三态渲染（有新版本 / 已是最新 / 本地构建跳过比较 / 暂时查不到）、pill 红点、
  进度条与弹窗日志、**关弹窗不中断任务**、「已就位 → 立即重启生效」的二次确认（正确报出运行实例数）。
- ⚠️ 端到端真机验证要等下一个带 `SHA256SUMS` 的 Release：v0.5.1 没有该资产，更新器会（按设计）
  拒绝安装并提示「打开 Release 页」。
