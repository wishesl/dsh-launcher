# DSH 启动器（Wails + React）需求文档

> 依据 `DSH版本查询与升级指南.md` 整理，2026-08-21

## 1. 是什么（What）

一个桌面 GUI 启动器，用 **Wails v2 + Go** 做后端、**React + TypeScript + Vite** 做前端，
帮助用户在**任意目录**以**指定版本**启动 DeepSeek Harness（DSH），并能**实时查询 npm 最新版**。

DSH 的启动方式本质是 `npx -y @deepseek-ai/dsh@<版本> web`（在某个工作目录里运行）。
启动器把"选目录 + 选版本 + 启动/停止 + 看版本"封装成可视化操作。

## 2. 为什么（Why）

指南里记录了两个痛点：
1. **npx 会优先命中启动目录里的本地 `node_modules` 副本**，导致"明明 npm 有新版本，本机却一直在跑旧版"。
2. 版本查询/升级要敲一堆命令行（`npm view` / `npx -y @deepseek-ai/dsh@latest web`），容易记错。

启动器价值：
- 一个实例 = **一个目录 + 一个版本**，多个实例互不干扰（对应指南"版本别混着用"）。
- 可视化显示 **npm 最新版 / 全部版本 / 发布时间 / 本地实际版本**，避免记忆偏差。
- 一键启动/停止，日志实时回显，不再手敲 npx 命令。

## 3. 怎么做（How）

### 架构
```
Wails 桌面壳（Go 后端）
 ├─ npm 版本查询模块：QueryLatest / QueryVersions / DetectLocalVersion
 ├─ 实例持久化模块：instances.json（%APPDATA%\DSHLauncher\）
 ├─ 进程管理模块：启动(exec.Command npx) / 停止(taskkill /T) / 日志推送(Events.Emit)
 └─ 绑定方法暴露给前端 window.go.main.App.*
前端 React 页面
 ├─ 实例列表（启动/停止/删除，状态指示灯）
 ├─ 添加实例表单（目录选择器 + 版本选择）
 ├─ 最新版本面板（latest / next / 版本历史表）
 └─ 运行日志面板
```

### 关键技术点（MVP 已初步验证）
| 难点 | 验证结果 |
|---|---|
| npm registry 网络可达 | ✅ `registry.npmjs.org` 与 `registry.npmmirror.com` 均可取到 latest=0.1.1-rc.2 |
| 版本列表 | ✅ 10 个版本，dist-tags: latest=next=0.1.1-rc.2 |
| 本地版本探测 | 读 `目录/node_modules/@deepseek-ai/dsh/package.json` 的 version 字段 |
| 进程启动/停止 | exec.Command + 流式输出 + taskkill /T /F 杀进程树 |
| 目录选择 | Wails runtime.OpenDirectoryDialog |

### 目录结构
```
dsh-launcher/
├── main.go / app.go / instances.go / dsh_query.go / dsh_process.go
├── wails.json
├── frontend/src/
│   ├── App.tsx
│   ├── components/InstanceList.tsx / InstanceForm.tsx / VersionPanel.tsx / LogPanel.tsx
│   └── ...
```

## 4. 关键决策
- 启动命令：`npx -y @deepseek-ai/dsh@<version> web`（版本由用户选择，默认 latest）。
- 版本来源优先级：官方 registry 优先，npmmirror 兜底（指南 2.1/2.2）。
- 配置持久化：`%APPDATA%\DSHLauncher\instances.json`。
- 停止实例：`taskkill /PID <pid> /T /F`（npx→node 进程树一并清理）。

## 5. 隐藏到托盘（2026-08-22 新增）
- 点击窗口关闭(X)：默认**隐藏到托盘**而非退出，进程与运行中的 DSH 实例不受影响。
- 系统托盘出现 DSH 图标：左键单击恢复窗口；右键菜单含 `显示主界面 / 隐藏 / 退出`。
- 顶栏新增 `最小化到托盘` 按钮 + `关闭时最小化到托盘` 开关（持久化到
  `%APPDATA%\DSHLauncher\settings.json`，默认开启）。
- 关键技术：Wails v2.10.2 在 Windows **无原生托盘**（`pkg/menu.TrayMenu` 仅 macOS），
  采用 `fyne.io/systray`（独立 goroutine 跑消息循环）与 Wails 共存；
  `OnBeforeClose` 返回 true + `runtime.WindowHide` 实现“点 X 隐藏”；
  托盘“退出”置 `quitting` 标志后 `runtime.Quit` 绕过拦截真正退出。
  托盘图标在 Windows 用 `build/windows/icon.ico`（LoadImage 需真 .ico），其他平台用 PNG。
